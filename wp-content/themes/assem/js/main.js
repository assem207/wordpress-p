/*const formComment = document.querySelectorAll('.comment-respond input');

const addClassFormControlToInputs = () => {

  formComment.forEach((el) => {

    el.classList.add('form-control');

  });

  document

    .querySelector('.comment-respond textarea')

    .classList.add('form-control');

};

document.addEventListener('DOMContentLoaded', addClassFormControlToInputs());*/